<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= title ? `${title} | PETWOOF` : "PETWOOF Admin" %></title>

  <link rel="stylesheet" href="/css/app.css" />
  <link rel="stylesheet" href="/css/pages/admin.css" />
  <% if (pageCss) { %>
    <link rel="stylesheet" href="<%= pageCss %>" />
  <% } %>
</head>

<body class="app">
  <!-- NAV ADMIN -->
  <header class="adminTop">
    <div class="container adminTop__inner">
      <a class="adminBrand" href="/admin">PETWOOF <span>ADMIN</span></a>

      <nav class="adminNav">

        <a class="pillBtn pillBtn--outline" href="/" title="Ir a tienda">Tienda</a>
        <button class="pillBtn" id="adminLogoutBtn" type="button">Cerrar sesión</button>
      </nav>
    </div>
  </header>

  <main class="page adminPage">
    <div class="container">
      <%- body %>
    </div>
  </main>

  <script src="/js/admin.js" defer></script>
  <% if (pageJs) { %>
    <script src="<%= pageJs %>" defer></script>
  <% } %>
</body>
</html>
