<header>
  <div class="topbar">
    <div class="container topbar__inner">
      <nav class="topbar__links" aria-label="Top links">
        <a href="#">Tracking Package</a>
        <a href="#">Sobre Nosotros</a>
        <a href="#">Contáctanos</a>
      </nav>
    </div>
  </div>

  <div class="nav">
    <div class="container nav__inner">
      <a class="nav__logo" href="/">WebbyFrames</a>

      <div class="nav__searchRow">
        <div class="search" role="search">
          <input type="search" placeholder="Buscar" aria-label="Buscar" />
          <svg class="search__icon" viewBox="0 0 24 24" fill="none">
            <path d="M10.5 18a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15Z" stroke="currentColor" stroke-width="2"/>
            <path d="M16.5 16.5 21 21" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>

        <button class="nav__catBtn" type="button">
          <span>Categorías</span>
          <svg viewBox="0 0 24 24" fill="none">
            <path d="M6 9l6 6 6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      </div>

      <div class="nav__icons" aria-label="User actions">
        <a class="iconBtn" href="/login" aria-label="Usuario">👤</a>

        <!-- ✅ click normal abre drawer, ctrl/cmd click abre /cart -->
        <a class="iconBtn cartIcon" href="/cart" aria-label="Carrito" data-cart-open>
          🛒
          <span class="cartBadge hide" id="cartBadge" aria-label="Items en carrito"></span>
        </a>
      </div>
    </div>
  </div>
</header>

<!-- ✅ Mini-carrito tipo drawer -->
<div class="cartDrawer" id="cartDrawer" aria-hidden="true">
  <div class="cartDrawer__backdrop" data-cart-close></div>

  <aside class="cartDrawer__panel" role="dialog" aria-modal="true" aria-label="Carrito">
    <header class="cartDrawer__head">
      <div class="cartDrawer__title">Carrito</div>
      <button class="cartDrawer__close" type="button" aria-label="Cerrar" data-cart-close>✕</button>
    </header>

    <div class="cartDrawer__body" id="cartDrawerBody">
      <!-- JS render -->
    </div>

    <footer class="cartDrawer__foot">
      <div class="cartDrawer__subtotal">
        <span class="muted">Subtotal</span>
        <strong id="cartDrawerSubtotal">$0.00</strong>
      </div>

      <div class="cartDrawer__cta">
        <a class="btn btn-primary" href="/cart">Ir a carrito</a>
      </div>
    </footer>
  </aside>
</div>
